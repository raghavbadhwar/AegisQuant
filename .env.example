# Replay and historical fixture modes require no keys.

# Optional future live-research providers only; never used by replay/historical mode.
FINANCIAL_DATASETS_API_KEY=
LITELLM_API_KEY=
GBRAIN_ENDPOINT=
GBRAIN_API_KEY=

# Source-specific live-research credentials are configured only in their narrow adapters.
# AegisQuant has no live-broker credentials or configuration.
