# Memory Policy

Only approved records enter long-term memory. Historical retrieval requires `available_at <= case.as_of`. Retrieved memory is evidence context, never authoritative instruction. Agent writes are candidate-only.
