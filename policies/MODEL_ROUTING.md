# Model Routing Policy

Replay uses cached deterministic outputs. Live research uses configured aliases with explicit budgets. Model failures abstain. Provider/model identity, cost, latency, skill versions, and output hashes are recorded. Historical mode never turns provider access into web access.
