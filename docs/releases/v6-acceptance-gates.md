# v6 Acceptance Gates

## Entry assessment — engineering implementation permitted

| Requirement | Evidence |
| --- | --- |
| isolated worktree | `/Volumes/RAGHAV2/Development_Projects/aegisquant-v6` |
| isolated branch | `upgrade/aegisquant-v6-investment-scientist` |
| base commit | `579ddc13c9dae455e87b13720c5b95fa96701096` |
| v4 causal-world-model surface | sealed domain pack, scenario, twin, experiment, uncertainty, counterfactual, scenario intelligence and microstructure contracts at the base |
| deterministic v4 validation | engineering evidence at base: `430 passed`; lint, format, mypy, lock and diff clean |
| v6 reuse assessment | existing experiment ledger, capability broker, candidate boundary, LangGraph harness, and VOI planner mapped in v6 architecture |
| v4 completion manifest required by the post-v5 prompt | `docs/releases/v4-causal-world-model-completion.md`, bound to base `579ddc1`; completed/engineering-only/release-gated |

The Git-retained manifest permits candidate-only v6 engineering implementation
after an independent read-only review confirms its commit map and disposition.
It is not an external original-record receipt and does not claim that v4 or v6
is released for empirical, investment, governance, or production use.

## v6 engineering acceptance

All conditions must be proven by tests, reviewed artefacts, and an independent
read-only audit:

1. A sealed programme contains at least two competing falsifiable hypotheses.
2. Every run has a sealed preregistration before its start and is recorded in
   the append-only ledger before a result is returned.
3. Data snapshots, splits, metrics, cost model, ledger, verification logic,
   risk, execution, and promotion remain locked from candidate/agent control.
4. The archive retains and searches negative and inconclusive results.
5. A verified claim binds an independent replication; a limited claim and an
   abstention cannot be rendered as stronger conclusions.
6. Proposer, verifier, approver, and replicator separation is fail-closed.
7. Research ranking is finite, reconciled, bounded by budget, and produces a
   documented stop for non-positive/redundant/budget/deadline conditions.
8. Teams and the progressive research tree preserve parentage, exact depth and
   budget limits, critique-before-run, and replication-only duplicate branches.
9. Same sealed inputs and deterministic fixture evaluator produce byte-identical
   history and report output.
10. No v6 module/import/view/CLI creates network I/O in historical/replay or
   capital/execution/risk/promotion authority.
11. Full repository gate and independent audit have no unresolved material finding.
12. `docs/releases/v6-dependency-license-review.md` records the exact locked
    dependency inventory, source/revision, licence or unknown status, approval,
    and required attribution; no unapproved dependency is imported.

## Release disposition and external gates

v6 remains `engineering-only` and `release-gated` unless independently governed
evidence exists. It cannot clear the existing blockers: approved
survivorship-safe PIT market/universe/corporate-action/delisting source,
governed archived-XBRL corpus evidence, an approved dated identifier-history source, hardened
N-PORT disclosure/raw-to-receipt binding, and externally retained append-only
original receipts. Local Yahoo/fixture output cannot establish accuracy,
calibration, outperformance, investment, eligibility, governance, or release.

No v7 work may start until v6 is independently accepted against this document.
No v6 result gains portfolio, risk, execution, broker, promotion, spending, or
external side-effect authority at any disposition.
